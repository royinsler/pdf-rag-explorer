# API Keys and secrets
.env

# Python environment
venv/
__pycache__/
*.pyc

# Local databases
chroma_db/

# Test/debug PDFs — never commit real documents used for local debugging
# (uploaded files can contain sensitive content, e.g. contracts/resumes)
test.pdf
*.debug.pdf
